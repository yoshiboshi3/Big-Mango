public readonly struct SpatialWindowState
{
    public readonly float X, Y, Z, Scale;
    public SpatialWindowState(float x, float y, float z, float scale)
    {
        X = x; Y = y; Z = z; Scale = scale;
    }
}
